# 第四篇第3章：连接复用、重连与请求恢复

## 承接范围与设计

沿用已确认的全书结构和第四篇第2章的交接安排，章节编号为 `part: 4`、`chapter: 3`，状态为 `draft`。标题为《Python Bridge 连接复用与请求恢复：从断线到可判定结果》。

正文依次讲解：Router 方法注册与连接的关系；连接与业务请求分开管理；单一连接管理者、代次和旧事件隔离；有上限的指数退避；未发送/可能已发送请求的不同恢复路径；结果账本、幂等键及重启后的证据；两个标准库实验；验证结果、常见问题与延伸阅读。

关键决定：

- `generation`、`NOT_APPLIED_FINAL` 和恢复决策是本书自定义应用模型，明确区别于 Arduino 官方 API。
- 旧连接响应不得覆盖新请求；历史请求可由稳定业务键和参数摘要对账。
- `NOT_FOUND` 不等于没有执行；超过业务期限禁止重新发送，但仍可记录迟到结果。
- 两个示例保存为实际 Python 文件，正文与文件保持一致；补充针对竞争、取消、重试上限、误关联和过期请求的测试。
- Fig-26 使用 Mermaid 状态图；优先尝试已存在的本地渲染器，不修改全局工具安装。
- 第二章提交 `44a2b1e` 已获授权推送；第三章完成后本地提交，保持本轮推送在写新章之前的顺序。

## 文件范围

- 正文：`book/第4篇_PythonBridge/第3章_Python_Bridge连接复用与请求恢复_从断线到可判定结果.md`
- 代码：`code/第4篇_PythonBridge/第3章_连接复用与请求恢复/`
- 图：`diagrams/uno-q-python-bridge-connection-recovery.mmd`，Fig-26
- 导航和来源：根 README、SUMMARY、第四篇 README、第二章交接链接、第四篇图示登记、resources/references.md
- 验证记录：本计划末尾

## 执行清单

- [x] 推送第二章并独立核验 GitHub main 为 `44a2b1e30b96a387d292afa803c53976a9ee67fa`。
- [x] 阅读写作规范、既有交接和相关官方资料。
- [x] 编写第三章、两个独立示例及针对性测试。
- [x] 同步 Fig-26、导航、来源和代码入口。
- [x] 运行示例、测试、链接与源码一致性检查，尝试图示渲染。
- [x] 审阅并修正问题，提交第三章。

## 验证记录

- Python 3.14.6：两个示例均运行成功，17 项针对性测试全部通过。
- `check_chapter.py`：元数据、两个示例精确同步、Python 3.10 AST、Mermaid、SVG、102 个内部链接全部通过。
- Mermaid CLI 11.12.0 已生成 Fig-26 SVG，并使用 Chrome 预览检查文字、连线和状态边界。
- 本地未使用全局安装；目标 Python 3.10 解释器、UNO Q 镜像、Router/Bridge、App Lab、MCU 和硬件实机验证尚未执行。
