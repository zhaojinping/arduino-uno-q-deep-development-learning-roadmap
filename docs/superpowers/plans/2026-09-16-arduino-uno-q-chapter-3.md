# Arduino UNO Q 深度开发学习路线：第 3 章硬件架构

## 目标

在现有第一篇第 1、2 章之后，完成出版质量的第 3 章《UNO Q 的硬件架构》，并把章节、Mermaid 源文件、目录、第一篇导读、根 README 和插图登记同步到可持续维护的 Markdown 仓库。

## 现状与约束

- 当前仓库根目录为 `C:\Users\华为\Documents\Codex\2026-09-15\referenced-chatgpt-conversation-this-is-an\Arduino UNO Q 深度开发学习路线`，分支为 `main`，远程为用户的公开 GitHub 仓库。
- 用户已授权在该 GitHub 仓库上持续维护和更新；本轮完成验证后可以提交并推送本轮文档变更。
- 第 1 篇固定目录为 `book/第1篇_认识UNOQ/`；现有章节命名、中文术语、front matter、Mermaid、图示占位和相对链接规则以 `docs/writing-guidelines.md` 为准。
- 现有 `resources/references.md` 已登记本章需要的官方产品页、UNO Q 数据表、UNO Q User Manual 和 `ArduinoCore-zephyr`；本轮不重复登记，不复制外部代码、图片或正文。
- 本章是硬件认知章，不承担完整数据表、逐针脚接线、电源设计或实机实验；对未经本次实机验证的内容必须写清“待硬件验证”。本章不新增可运行代码时，明确写出“本章无可运行代码”，不伪造运行结果。
- 本轮只扩展一个后续章节；下一章及第一篇其余章节继续留作后续会话，避免一次性制造未核验的大量内容。

## 设计决定

1. 章节文件采用 `book/第1篇_认识UNOQ/第3章_UNO_Q的硬件架构.md`，标题使用首次出现的完整产品名规则，后文统一使用 `UNO Q`。
2. 章节围绕“计算、实时 I/O、连接与扩展、电气边界”四层组织，而不是按数据表逐项罗列；明确 MPU、MCU 和逻辑协同路径的区别。
3. 只使用一张主 Mermaid 架构图 `diagrams/uno-q-hardware-map.mmd`，图中虚线只表示逻辑协同关系，不把 Arduino Bridge/RPC 误写成单一物理连线；正文保留 Fig-04 图示占位并登记未来 SVG 的来源边界。
4. 操作部分采用“资料识读实验”：读者根据官方产品页、User Manual 和数据表把任务分配到计算侧、实时侧、扩展侧或电气核验侧；验证结果只记录可由文档复核的结论，不将纸面练习写成实测。
5. 章节必须链接第 2 章及已经存在的第二至第五篇入口；不链接尚未创建的具体章节文件。

## 来源事实基线

- 官方产品页核对：QRB2210 MPU、Debian Linux、STM32U585 Cortex-M33、MPU/MCU 的高层定位、WCBN3536A Wi-Fi 5/Bluetooth 5.1、UNO headers、Qwiic、底部高速连接器和 IDE/App Lab 的分工。
- 官方 UNO Q 数据表核对：主要硬件资料、电气域提示、扩展连接器和只应在数据表层面呈现的边界；对 1.8 V MPU 侧与 3.3 V MCU 侧必须给出“不可凭名称直接互接”的安全提示。
- 官方 User Manual 核对：UNO form factor、USB-C 供电/连接的高层说明、IDE 只编程 MCU、App Lab 的双侧工作流；具体操作仍留给后续篇章。
- `ArduinoCore-zephyr` 核对：UNO Q 的 Zephyr 目标、`arduino_uno_q_stm32u585xx` 变体和 Arduino 草图的 MCU 目标属性；不要把 core 配置字段当作硬件实测。

## 实施任务

### Task 1：写作第 3 章与图示源文件

**交付文件**

- `book/第1篇_认识UNOQ/第3章_UNO_Q的硬件架构.md`
- `diagrams/uno-q-hardware-map.mmd`

**章节最低内容**

- 完整 front matter：`title`、`part: 1`、`chapter: 3`、`status`、`last_verified: 2026-09-16`。
- 按规范包含：学习目标、本章导读、背景与边界、正文分节、操作或实验、验证结果、常见问题、本章小结、交叉引用与延伸阅读、来源与验证。
- 正文至少解释：从传统开发板到计算平台的视角、MPU（QRB2210 + Debian Linux）、MCU（STM32U585 + Zephyr/Arduino Sketch）、无线与扩展资源、供电/USB-C 的高层边界、跨处理器逻辑协同与电气域风险。
- 说明“有接口不等于任意可用”：UNO headers、Qwiic、底部高速连接器、接口复用、软件支持和电气条件都需要分别核验；不扩写未经来源支持的逐针脚接线结论。
- 操作部分使用纸面资料识读，不写成硬件实测；本章明确无可运行代码，或在代码边界处明确说明代码留给后续章节。
- 插入 Fig-04 单行占位，内容与 `diagrams/uno-q-hardware-map.mmd` 一致，并说明 Mermaid 源文件和未来 SVG 的原创重绘边界。
- FAQ 至少覆盖：QRB2210 是否取代 MCU、UNO headers 是否可默认按 5 V 处理、MPU/MCU GPIO 是否可直接互接、Qwiic 是否等于任意 I2C 接线、硬件存在是否等于软件支持。

**图示最低内容**

- 使用明确的 `flowchart LR` 或 `flowchart TB`。
- 展示 `Arduino UNO Q`、`QRB2210 MPU`、`STM32U585 MCU`、无线模块、UNO/Qwiic/底部高速扩展资源和 `Arduino Bridge / RPC` 的关系。
- 逻辑协同使用虚线并标注“逻辑关系，不表示单一物理连线”；不制造不存在的时序或物理总线结论。

### Task 2：同步可导航入口和图示登记

**交付文件**

- `SUMMARY.md`
- `book/第1篇_认识UNOQ/README.md`
- `images/第1篇_认识UNOQ/README.md`
- `README.md`

**验收要点**

- `SUMMARY.md` 在第 2 章之后加入第 3 章，并保持稳定阅读顺序。
- 第一篇 README 将硬件架构占位替换为第 3 章链接，保持后续软件架构和第一个实验入口的预留说明。
- 图示登记新增唯一 `Fig-04`，包含目标 SVG、Mermaid 源文件、正文锚点、内容要求和原创重绘/许可边界；不创建空图片文件。
- 根 README 的进度、文件清单和验证边界反映第 3 章及新 Mermaid 文件；不把纸面资料验证写成实机通过。

### Task 3：整体验证、审阅和交付

- 检查新章节 front matter、标题/文件名/目录一致性、章节必备段落、唯一图号、Mermaid 源文件与正文图示一致性。
- 检查所有 Markdown 链接的目标存在性，重点检查新增跨篇入口、图片登记链接和新章节锚点。
- 检查来源是否都已在 `resources/references.md` 登记，外部事实是否能回指官方来源，是否存在把推测或纸面练习写成实测的表述。
- 检查工作区无无关变更、分支与远程同步；通过后创建小步提交并推送 `main`，再核对本地与远程提交哈希一致。
- 如审阅发现问题，只做一轮聚焦修复，然后重复完整验证；未完成硬件实机验证仍保留为明确限制。

## 验收标准

- 第 3 章可从 `SUMMARY.md` 和第一篇 README 进入，且章节内链不指向不存在的目标。
- Mermaid 文件可独立追溯，正文内联图（如保留）与 `.mmd` 内容一致；Fig-04 只使用规范占位格式。
- 章节事实边界、1.8 V/3.3 V 风险、MPU/MCU 责任域和扩展接口边界清楚，读者不会把 UNO Q 当作单处理器 UNO 或把所有接口当作同一电气域。
- 纸面资料识读实验有步骤、预期结论、故障/误读排查和验证方式；无虚构硬件输出。
- `git status` 干净，远程 `main` 包含本轮提交，交付说明包含提交哈希、验证范围和未完成项。

## 计划自审

- 已核对现有章节和写作规范；没有重新发明第一篇路径或章节编号。
- 事实以当前核验的 Arduino 官方资料为基线，社区资料不进入本章事实论证。
- 任务边界按“正文/图示”和“索引同步”拆分，文件集合不重叠；整体验证由控制代理统一执行。
- 计划明确了无需新增参考资料行、无需可运行代码和不得宣称实机验证的约束。
