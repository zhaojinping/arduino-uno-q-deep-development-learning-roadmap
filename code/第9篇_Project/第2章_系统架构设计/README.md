# 第九篇第2章：系统架构设计

本目录保存第九篇第2章对应的离线文档契约测试。测试读取本仓库内的 Markdown、Mermaid 与导航文件，并在当前主机进程内执行正文中的纯状态分类函数，验证来源时钟、主机时钟和持久化门禁。函数不含 I/O；测试不连接 Arduino UNO Q、传感器、网络服务、Broker 或外部 API，不读取凭据，也不访问运行时数据文件、缓存或持久化后端。

在仓库根目录运行：

```powershell
python -B -m unittest discover -s "code/第9篇_Project/第2章_系统架构设计" -p "test_*.py" -v
```

测试通过表示书稿结构/内容边界、图源一致性、目录登记及纯函数在主机上的指定输入行为满足当前测试；这是 `HOST_TEST`，不表示示例已在目标板运行，也不模拟传感器、Bridge 或持久化后端。实机状态由正文“验证结果与当前证据”表单独记录。
