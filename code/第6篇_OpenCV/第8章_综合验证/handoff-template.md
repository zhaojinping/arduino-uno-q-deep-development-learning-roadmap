# 第六篇第8章：视觉链路证据交接模板

本模板记录离线验证与现场验证的**不同证据等级**。复制使用时填写真实运行信息，不应把下面的本机样例描述为 Arduino UNO Q 上的结果。

## A. 运行身份与环境

| 项目 | 填写值 |
| --- | --- |
| 验证日期/操作者 | 待填 |
| 代码版本（Git 提交） | 待填 |
| Python、OpenCV、NumPy 版本 | 待填 |
| 数据来源 | 合成 / 本地文件 / 具体相机与驱动（逐项写明） |
| `source`、`run_id` | 待填；说明由谁生成、如何防复用 |
| 采集时钟与审查时钟 | 待填；未确认同一时钟域则不得宣称绝对时效合格 |
| HSV、面积、跳变、年龄、ROI 与意图限值 | 待填，并保存参数来源 |

## B. 离线记录复核

保存 `run_visual_handoff.py` 的九条原始 JSONL 与执行命令、解释器版本、`git rev-parse HEAD` 的输出；不要只保留一张终端截图。若改变示例，记录新输入与期望结果，不能沿用本章实测表。

| 场景 | 期望检查点 | 本次结果/原始记录索引 |
| --- | --- | --- |
| 前两帧 | `TENTATIVE`；`BLOCKED` | 待填 |
| 第3帧 | `STABLE`；仅 `PREVIEW_ONLY` | 待填 |
| 第3帧错会话探针 | `review_probe`；`source_or_session_mismatch`；不计入帧号 | 待填 |
| 第4帧目标丢失 | `LOST`；无预览 | 待填 |
| 第5、6帧重新起算 | `TENTATIVE`；无预览 | 待填 |
| 第7帧过期 | `STABLE` 但 `stale_or_future_frame`；无预览 | 待填 |
| 第8帧跳变 | 有像素候选、`REJECTED_JUMP`、时间门质心为空；无预览 | 待填 |
| 自动测试 | 六项集成测试及全仓库代码测试的命令/结果 | 待填 |

## C. 现场/硬件验收缺口

默认均为**未验证**。除非附有独立证据，否则不可因离线 `PREVIEW_ONLY` 而改为“通过”。

| 验收门 | 状态与证据位置 |
| --- | --- |
| UNO Q 具体板卡、镜像、相机、载板、采集后端兼容 | 未验证 |
| 真实采集帧格式、掉帧、乱序、曝光/光照、误报率 | 未验证 |
| 时间戳同源与端到端延迟分布、超时处置 | 未验证 |
| 操作者身份、授权、会话抗重放、幂等与审计 | 未验证 |
| Bridge/Router 消息到达与未知结果对账 | 未验证 |
| MCU 独立限幅、安全态、看门狗与物理急停 | 未验证 |
| 实际输出、接线、电气和独立物理测量 | 未验证 |
| 图示 SVG 渲染与视觉审阅 | 未验证 |

## D. 交接结论

- 本次可以主张的范围：待填；明确写“合成离线”“本地文件”或“指定硬件组合”。
- 阻断/不确定事项：待填。任何未知执行状态都不得自动重试为新动作。
- 下一次验证所需设备、权限、负责人和停止条件：待填。
- 审查人、日期及复核意见：待填。
